# WokGen apps/web - example env file (placeholders only).
# Copy this to .env and fill values for local development.
# Do NOT commit .env with real secrets.

# Database: PostgreSQL is required (schema uses postgresql provider).
# Options for local dev:
#   1. Use your Render Postgres external URL (copy from Render dashboard after deploy)
#   2. Free Neon DB: https://neon.tech → create project → copy connection string
#   3. Local Docker: docker run -d -p 5432:5432 -e POSTGRES_USER=wokgen -e POSTGRES_PASSWORD=dev -e POSTGRES_DB=wokgen postgres:15
DATABASE_URL="postgresql://wokgen:dev@localhost:5432/wokgen"

# Base URL used by Next.js metadata and sitemap generation
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Disable Next telemetry if you prefer
NEXT_TELEMETRY_DISABLED=1

# Optional provider keys (uncomment and set only for testing; do not commit)
# TOGETHER_API_KEY="sk-xxxxx"
# REPLICATE_API_TOKEN="r8.xxxxx"
# FAL_API_KEY="fl_xxxxxx"

# ComfyUI local host/port (if using local ComfyUI)
# COMFYUI_HOST="http://localhost"
# COMFYUI_PORT=8188

# Misc (optional)
# NEXTAUTH_SECRET="some_long_random_secret_for_nextauth_if_enabled"
