# Production example env for WokGen (placeholders only)
# Copy to .env.production (or use your secrets manager) and fill values before deploying.
# Do NOT commit .env.production with real secrets.

# Database: Use PostgreSQL in production. Example:
# DATABASE_URL="postgresql://wokgen_user:change_me@db:5432/wokgen_db"
DATABASE_URL="postgresql://<user>:<pass>@<host>:5432/<db>"

# Public base URL for sitemap / metadata
NEXT_PUBLIC_BASE_URL="https://wokspec.org/wokgen"

# Next.js production toggles
NEXT_TELEMETRY_DISABLED=1
NODE_ENV=production

# NextAuth or session secret (if used)
NEXTAUTH_SECRET="<long_random_string>"

# Provider keys (server-side if you want centralized keys; or instruct using BYOK in browser)
# TOGETHER_API_KEY="<together_key>"
# REPLICATE_API_TOKEN="<replicate_token>"
# FAL_API_KEY="<fal_key>"

# Local ComfyUI (if you run ComfyUI in the same network)
# COMFYUI_HOST="http://comfyui"
# COMFYUI_PORT=8188

# Optional: set the port the container should expose (defaults to 3000)
PORT=3000

# Healthcheck endpoint for orchestrators
HEALTHCHECK_URL="/api/health"
