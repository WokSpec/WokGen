# WokGen (.env.example)
# WokGen is a monorepo — the web app lives in apps/web/.
#
# For local development, copy apps/web/.env.example to apps/web/.env.local:
#   cp WokGen/apps/web/.env.example WokGen/apps/web/.env.local
#
# The key required variables are listed below. See apps/web/.env.example for
# the full list including optional AI/CDN/observability providers.

# === Database (required) ===
DATABASE_URL=postgresql://wokgen:dev@localhost:5432/wokgen
# Neon option: postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/wokgen?sslmode=require
# Quick local Docker DB:
#   docker run -d -p 5432:5432 -e POSTGRES_USER=wokgen -e POSTGRES_PASSWORD=dev -e POSTGRES_DB=wokgen postgres:15

# === Auth (NextAuth v5 — required) ===
# Generate: openssl rand -base64 32
AUTH_SECRET=generate_with_openssl_rand_base64_32
AUTH_URL=http://localhost:3000

# GitHub OAuth (recommended for local dev)
# Create at: https://github.com/settings/applications/new
# Callback URL: http://localhost:3000/api/auth/callback/github
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# === Redis / BullMQ (optional — jobs run in-process if unset) ===
# REDIS_URL=redis://localhost:6379
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# === AI Providers (optional for local dev) ===
# GROQ_API_KEY=          # Free at console.groq.com — required for Eral AI chat
# OPENAI_API_KEY=        # TTS + text generation
# REPLICATE_API_TOKEN=   # HD image generation
# ELEVENLABS_API_KEY=    # Primary TTS (highly recommended)

# === Stripe (optional — billing disabled if unset) ===
# STRIPE_SECRET_KEY=sk_test_
# STRIPE_WEBHOOK_SECRET=whsec_

# === App ===
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_TELEMETRY_DISABLED=1

# See WokGen/apps/web/.env.example for the full list of all optional providers.
