# Production example env for WokGen (placeholders only)
# On Vercel: set these in the Vercel dashboard (Project → Settings → Environment Variables).
# Do NOT commit real secrets.

# Database: Neon (free serverless Postgres). Get your URL from console.neon.tech
# Format: postgresql://user:pass@ep-xxx-xxx.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://<user>:<pass>@ep-xxx-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# Public base URL — must match your Vercel custom domain
NEXT_PUBLIC_BASE_URL="https://wokgen.wokspec.org"

# Next.js production toggles
NEXT_TELEMETRY_DISABLED=1
NODE_ENV=production

# NextAuth or session secret (if used) — generate with: openssl rand -base64 32
NEXTAUTH_SECRET="<long_random_string>"

# Provider keys (server-side centralized keys; or use BYOK flow in the browser)
# TOGETHER_API_KEY="<together_key>"
# REPLICATE_API_TOKEN="<replicate_token>"
# FAL_API_KEY="<fal_key>"

# Local ComfyUI (only if running ComfyUI alongside the app)
# COMFYUI_HOST="http://comfyui"
# COMFYUI_PORT=8188
